

for task in self.notCompletedTasks {
               print(task.projref!)
               vProjectRef += task.projref! + "|"
           }

##-- 아예 코어데이터 프레임워크에 해당 리소스를 넣어 디자인 한다.

#-- 네트워크 연결기능을 추가(20200904)
    => Singleton/Combine 방식을 활용하여 처리


